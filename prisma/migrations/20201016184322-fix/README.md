# Migration `20201016184322-fix`

This migration has been generated by cipriano98 at 10/16/2020, 3:43:24 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Agency" DROP COLUMN "ativo",
ADD COLUMN "active" boolean   NOT NULL DEFAULT true

ALTER TABLE "public"."Company" DROP COLUMN "ativo",
ADD COLUMN "active" boolean   NOT NULL DEFAULT true

ALTER TABLE "public"."User" DROP COLUMN "ativo",
ADD COLUMN "active" boolean   NOT NULL DEFAULT true
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201015145657-fix..20201016184322-fix
--- datamodel.dml
+++ datamodel.dml
@@ -1,9 +1,9 @@
 // https://www.meucurriculoperfeito.com.br/criar-curriculo/section/cntc
 // & https://www.prisma.io/docs/reference/tools-and-interfaces/prisma-schema/relations
 datasource db {
     provider = "postgresql"
-    url = "***"
+    url = "***"
 }
 generator client {
     provider = "prisma-client-js"
@@ -26,9 +26,9 @@
     securitykey       String @default(uuid())
     fullname         String
     secret           String?
-    ativo            Boolean @default(true)
+    active           Boolean @default(true)
     email            String  @unique
     cpf              String  @unique
     role             Role    @default(CANDIDATE)
     datebirth        String?
@@ -76,30 +76,30 @@
     userid Int
 }
 model Company {
-    id              String  @id @default(uuid())
+    id              String   @id @default(uuid())
     name            String
-    registrofederal String  @unique
+    registrofederal String   @unique
     site            String?  @unique
     links           String[]
     labellinks      String[]
-    ativo           Boolean @default(true)
-    role            Role    @default(EMPLOYER)
+    active          Boolean  @default(true)
+    role            Role     @default(EMPLOYER)
     createdat DateTime @default(now())
     updatedat DateTime @updatedAt
 }
 model Agency {
-    id              String  @id @default(uuid())
+    id              String   @id @default(uuid())
     name            String
-    registrofederal String  @unique
-    site            String  @unique
+    registrofederal String   @unique
+    site            String   @unique
     links           String[]
     labellinks      String[]
-    ativo           Boolean @default(true)
-    role            Role    @default(VACANCYDISTRIBUTOR)
+    active          Boolean  @default(true)
+    role            Role     @default(VACANCYDISTRIBUTOR)
     createdat DateTime @default(now())
     updatedat DateTime @updatedAt
 }
```


